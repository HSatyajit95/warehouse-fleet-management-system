# Copy to docker/.env (gitignored) and adjust as needed.
# docker compose automatically reads docker/.env when run from this directory.

# Number of robots fleet-server tracks (must match how many robot agents
# you're running on the host, e.g. `ros2 launch fms_navigation bringup.launch.py num_robots:=N`).
NUM_ROBOTS=4
